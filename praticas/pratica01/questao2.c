#include <stdio.h>

int main(){

  return 0;
  float base;
  float altura;

  base = 2.0f; //2.0f isso float, 2.0 isso double
  altura = 3.0f;

  base * altura / 2;

  float area = base * altura / 2;

  printf("A area do triangulo é: %f\n", area);
  
  return 0;
  
  }